{
  "name": "resalelane",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check site/app.js && node --check site/cart-logic.js && node --test"
  }
}
