{
  "name": "resalelane",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "node --check site/app.js && node --check site/cart-logic.js && node --check server/email-templates.js && node --check worker/index.js && node --check worker/contact.js && node --test && npm run build",
    "worker:deploy": "wrangler deploy",
    "worker:check": "wrangler deploy --dry-run"
  }
}
